import React from 'react';
import {SearchForm, SearchInput} from "./SearchBarStyle";

const Search = () => {
    return (
        <>
            <SearchForm>
                <SearchInput placeholder="Search your meals"/>
            </SearchForm>
        </>
    );
};

export default Search;
